input:
  mode: rgbd_dataset
  source: data/samples/tum_freiburg1_xyz

depth:
  source_mode: input
  output_mode: raw
  postprocess:
    enabled: false

slam:
  mode: groundtruth

semantics:
  enabled: false

ros2:
  enabled: false

output:
  output_dir: data/outputs/benchmarks/tum_gt_depth_gt_pose
  save_rgb_snapshot: true
  save_depth_snapshot: true
  save_pointcloud: true
  save_trajectory: true
